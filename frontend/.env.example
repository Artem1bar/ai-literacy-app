# Copy to frontend/.env.local (Vite reads it automatically; it is git-ignored).

# Base URL of the FastAPI backend. Defaults to http://localhost:8000 when unset.
# Point it at your deployed backend when building for production.
VITE_API_URL=http://localhost:8000
